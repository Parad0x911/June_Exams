<script>
// 1st person:
const person = {
  firstName: "Guillaume",
  lastName: "LEGOULLON",
  win: 2,
  lose: 11,
};

// Display some data from the object:
document.getElementById("demo").innerHTML =
person.firstName + person.lastName + " Won about " + person.win + " And lost " + person.lose;


// 2nd person:
const person = {
  firstName: "Demian",
  lastName: "DESAINTE",
  win: 7,
  lose: 21,
};

// Display some data from the object:
document.getElementById("demo").innerHTML =
person.firstName + person.lastName + " Won about " + person.win + " And lost " + person.lose;

// "3rd" person:
const person = {
  firstName: "Michael",
  lastName: "Rosen",
  win: 25,
  lose: 5,
};

// Display some data from the object:
document.getElementById("demo").innerHTML =
person.firstName + person.lastName + " Won about " + person.win + " And lost " + person.lose;

// "4th" person:
const person = {
  firstName: "Donald",
  lastName: "Trump",
  win: 9,
  lose: 11,
};

// Display some data from the object:
document.getElementById("demo").innerHTML =
person.firstName + person.lastName + " Won about " + person.win + " And lost " + person.lose;

// "5th" person:
const person = {
  firstName: "Calvin",
  lastName: "Broadus",
  win: 4,
  lose: 20,
};

// Display some data from the object:
document.getElementById("demo").innerHTML =
person.firstName + person.lastName + " Won about " + person.win + " And lost " + person.lose;

</script>